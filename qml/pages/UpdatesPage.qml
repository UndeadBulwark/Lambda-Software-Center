import QtQuick
import LambdaSoftwareCenter

Rectangle {
    id: updatesPage
    color: Theme.bgPrimary

    Text {
        anchors.centerIn: parent
        text: "Updates"
        font.pixelSize: 13
        color: Theme.textTertiary
    }
}
